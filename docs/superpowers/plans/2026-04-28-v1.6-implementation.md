# V1.6 — Skills Injection & Worker Subagents Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the eleven forensic findings from `2026-04-28-v1.6-skills-and-worker-subagents.md` by (a) injecting the 16 existing + 4 new SKILL.md packs into stage / synthesis / reviewer / audit prompts via a `SkillLoader`; (b) fanning each stage out to 2–4 named worker subagents executing in parallel under `asyncio.Semaphore`, each with a dedicated worker prompt and merging into a stage-level summary; (c) replacing the single LLM-call stage with a true ReAct loop binding `web_search`+`rag_search`; (d) adding per-stage reviewer prompts, per-stage `max_retries` overrides, and run lifecycle columns + heartbeat + recovery + per-run timeout; (e) backfilling 305 stale `running` rows.

**Spec:** `docs/superpowers/specs/2026-04-28-v1.6-skills-and-worker-subagents.md`

**Branch:** `feat/v1.6-skills-and-workers` (worktree `.worktrees/v1.6/`)

**Tech Stack:** Python 3.12, LangGraph, LangChain, SQLAlchemy async, Alembic, Postgres, FastAPI, pytest-asyncio; Next.js 16 + React 19 + Tailwind v4 frontend.

**Per-task discipline (mandatory for every task):**
1. TDD: failing test → run it → minimal implementation → run again → pass.
2. After each task: `CHECK_BUILD=1 bash scripts/check.sh` must be green.
3. Conventional commit per task.
4. Two-stage review (subagent-driven-development) between tasks: spec review then quality review.

---

## File Structure

### New (engine core)
- `backend/app/agents/_engine/skills.py` — `SkillLoader` (`load_skill`, `render_skills_block`, `inject_skills`).
- `backend/app/agents/_engine/workers.py` — worker fanout helpers (`run_workers_parallel`, merge step).
- `backend/app/agents/_engine/recovery.py` — stale-run reaper + heartbeat publisher.
- `backend/alembic/versions/0010_run_lifecycle_columns.py` — add `started_at`, `completed_at`, `heartbeat_at`.
- `backend/scripts/backfill_stale_runs.py` — one-off idempotent backfill.

### New (skills, 4 packs)
- `backend/app/skills/market-sizing/SKILL.md`
- `backend/app/skills/evidence-discipline/SKILL.md`
- `backend/app/skills/unit-economics/SKILL.md`
- `backend/app/skills/competitive-intelligence/SKILL.md`

### New (per-type per-stage reviewer prompts, 15 files)
- `backend/app/agents/market_entry/prompts/reviewer_stage{1_foundation,2_competitive,3_risk,4_demand,5_strategy}.md`
- `backend/app/agents/pricing/prompts/reviewer_stage{1_value,2_segments,3_competitive,4_models,5_rollout}.md`
- `backend/app/agents/profitability/prompts/reviewer_stage{1_revenue,2_cost,3_margin,4_competitor,5_levers}.md`

### New (worker prompts, 37 net-new + 7 reused)
- market_entry: 7 existing under `prompts/stage{1,2,3}/` reused; **add** `prompts/stage3_risk/{regulatory_risk.md, operational_risk.md}` (2), `prompts/stage4_demand/{demand_drivers.md, willingness_to_pay.md, segment_priority.md}` (3), `prompts/stage5_strategy/{go_to_market.md, partnerships.md, milestones.md}` (3) → 8 net-new.
- pricing: `prompts/stage1_value/`, `stage2_segments/`, `stage3_competitive/`, `stage4_models/`, `stage5_rollout/` × 3 each → 15 net-new.
- profitability: `prompts/stage1_revenue/`, `stage2_cost/`, `stage3_margin/`, `stage4_competitor/`, `stage5_levers/` × 3 each → 15 net-new.

### Modified (engine)
- `backend/app/agents/_engine/profile.py` — extend `ProfileStage` (`required_skills`, `workers`, `max_retries`); extend `ConsultingProfile` (`framing_skills`, `synthesis_skills`, `audit_skills`, `reviewer_skills_per_stage`, `reviewer_prompt_for_stage`); add `WorkerSpec` dataclass; extend `validate()`.
- `backend/app/agents/_engine/nodes/stage.py` — implement ReAct tool loop; integrate worker fanout when `workers` non-empty; emit no-tool-use warning.
- `backend/app/agents/_engine/nodes/reviewer.py` — per-stage prompt resolution + per-stage skill injection.
- `backend/app/agents/_engine/nodes/synthesis.py` — inject `synthesis_skills`; add path-normalization for known synonyms.
- `backend/app/agents/_engine/nodes/audit.py` — inject `audit_skills`; reuse normalizer.
- `backend/app/agents/_engine/nodes/framing.py` — inject `framing_skills`.
- `backend/app/agents/_engine/state.py` — add `worker_outputs: dict[str, dict[str, Any]]`.
- `backend/app/agents/_engine/edges.py` — read per-stage `max_retries` override.
- `backend/app/agents/_engine/graph.py` — pass `tools_factory` and per-stage worker config through to nodes.

### Modified (profiles, runtime, frontend)
- `backend/app/agents/market_entry/profile.py` — populate `required_skills`, `workers`, per-stage reviewer prompts, skills.
- `backend/app/agents/pricing/profile.py` — same; switch reviewer prompt package to `app.agents.pricing.prompts`.
- `backend/app/agents/profitability/profile.py` — same; switch reviewer prompt package to `app.agents.profitability.prompts`.
- `backend/app/models/run.py` — add `started_at`, `completed_at`, `heartbeat_at` columns.
- `backend/app/workers/run_worker.py` — start heartbeat task on run start; honour `RUN_TIMEOUT_SECONDS`; call recovery sweeper on startup.
- `backend/app/main.py` — invoke `recovery.sweep_stale_runs()` once during lifespan startup.
- `backend/app/core/config.py` — add `run_timeout_seconds`, `heartbeat_interval_seconds`, `stale_run_threshold_seconds`, `worker_concurrency`, `react_max_iterations`.
- `backend/app/api/runs.py` — strict scripted-model guard (raise if `model_factory` ever sentinel in non-test mode).
- `backend/app/agents/llm.py` — accept `tools` param for ReAct binding (passthrough to `bind_tools`).
- `backend/app/agents/tools/__init__.py` — expose `build_tools_factory(run_id)` returning `[web_search, rag_search]` bound to run context.
- `frontend/lib/useAgentStates.ts` — split agent ids on `.`; attach worker children to parent stage node.
- `frontend/components/AgentGraph.tsx` — render collapsible worker child nodes (mini-cards) under stage parent.

### New (tests, 17 net-new)
- `backend/tests/unit/test_skill_loader.py`
- `backend/tests/unit/test_profile_extensions.py`
- `backend/tests/unit/test_per_stage_max_retries.py`
- `backend/tests/unit/test_path_normalization.py`
- `backend/tests/integration/test_stage_node_react.py`
- `backend/tests/integration/test_worker_fanout.py`
- `backend/tests/integration/test_per_stage_reviewer.py`
- `backend/tests/integration/test_run_lifecycle_columns.py`
- `backend/tests/integration/test_run_recovery.py`
- `backend/tests/integration/test_run_timeout.py`
- `backend/tests/integration/test_backfill_stale_runs.py`
- `backend/tests/integration/test_scripted_model_guard.py`
- `backend/tests/integration/test_market_entry_v16_smoke.py`
- `backend/tests/integration/test_pricing_v16_smoke.py`
- `backend/tests/integration/test_profitability_v16_smoke.py`
- `frontend/__tests__/useAgentStates.workers.test.tsx`
- `frontend/__tests__/AgentGraph.workers.test.tsx`

### Modified (docs)
- `AGENTS.md` — note V1.6 pipeline shape, skill injection contract, worker fanout, run lifecycle columns.
- `backend/app/agents/agent.md` — describe new `_engine/skills.py`, `_engine/workers.py`, ReAct stage loop, per-stage reviewer.
- `backend/app/skills/agent.md` (create if absent) — list 20 packs and how stages reference them.

---

## Tasks

> Each task: write failing test(s) → run → minimal implementation → run → green → commit (`feat(v1.6): …`) → spec-review subagent → quality-review subagent → fix → next task.

### Task 1 — Spec doc (DONE)
- [x] Write `docs/superpowers/specs/2026-04-28-v1.6-skills-and-worker-subagents.md`.

### Task 2 — Plan doc (this file, DONE on commit)
- [ ] Commit this plan as `docs(v1.6): add implementation plan`.

### Task 3 — `Settings` extensions
- [ ] Tests: `tests/unit/test_settings_v16.py` (new five fields default values + env override).
- [ ] Add fields to `app/core/config.py`.
- [ ] Commit `feat(v1.6): add run lifecycle and worker config settings`.

### Task 4 — 4 new skill packs
- [ ] Tests: assert each new SKILL.md exists, has frontmatter `name:` matching slug, ≥40 lines body.
- [ ] Author `market-sizing`, `evidence-discipline`, `unit-economics`, `competitive-intelligence` SKILL.md (80–150 lines each, mirroring `strategic-analysis` structure: frontmatter + Sections + Worked Example + Anti-Patterns).
- [ ] Commit `feat(v1.6): add four new consulting skill packs`.

### Task 5 — `SkillLoader`
- [ ] Tests `tests/unit/test_skill_loader.py`: frontmatter strip; lru_cache hit; missing-slug raises `FileNotFoundError`; empty-tuple passthrough; ordering preserved; markdown header injected.
- [ ] Implement `app/agents/_engine/skills.py` (`load_skill`, `render_skills_block`, `inject_skills`).
- [ ] Commit `feat(v1.6): add SkillLoader for prompt injection`.

### Task 6 — `ProfileStage` / `ConsultingProfile` / `WorkerSpec` extensions
- [ ] Tests `tests/unit/test_profile_extensions.py`: defaults empty; `validate()` reads every referenced skill + every per-stage reviewer prompt + every worker prompt; missing entry raises with clear message; existing profiles still validate.
- [ ] Edit `_engine/profile.py`: add `WorkerSpec`, extend `ProfileStage` & `ConsultingProfile` with new fields, extend `load_worker_prompt(stage_slug, worker_slug)`, extend `validate()`.
- [ ] Confirm `market_entry` / `pricing` / `profitability` profiles still validate unchanged.
- [ ] Commit `feat(v1.6): extend Profile schema with skills and worker specs`.

### Task 7 — Per-stage `max_retries`
- [ ] Tests `tests/unit/test_per_stage_max_retries.py`: route uses override when set; falls back to `DEFAULT_MAX_STAGE_RETRIES` when None.
- [ ] Edit `_engine/edges.py` and `_engine/graph.py` to thread per-stage value into `make_route_after_reviewer`.
- [ ] Commit `feat(v1.6): per-stage max_retries override`.

### Task 8 — Path normalization helper
- [ ] Tests `tests/unit/test_path_normalization.py`: `stage_3_risk` ↔ `stage3_risk`, `risk.md` ↔ `findings.md`, original paths preserved on read.
- [ ] Implement `_engine/paths.py::normalize_artifact_path(path: str) -> set[str]` returning canonical + synonym set.
- [ ] Wire into synthesis & audit context builders.
- [ ] Commit `feat(v1.6): normalize artifact path synonyms in synthesis & audit`.

### Task 9 — ReAct loop in stage node (no workers yet)
- [ ] Tests `tests/integration/test_stage_node_react.py`: with scripted model emitting two tool_calls then final structured output, stage runs 3 iterations, both tools invoked, evidence rows written, no-tool path emits soft warning.
- [ ] Extend `FakeChatModel` (`tests/_helpers/fake_model.py`) with `tool_calls` script entry + `bind_tools()` honoured.
- [ ] Edit `_engine/nodes/stage.py`: implement loop, bind `tools_factory(run_id)`, enforce `react_max_iterations`, finalize via `with_structured_output(StageOutput)`, inject stage `required_skills`.
- [ ] Pass `tools_factory` through `build_consulting_graph`.
- [ ] Commit `feat(v1.6): ReAct tool loop in stage node`.

### Task 10 — Worker fanout
- [ ] Tests `tests/integration/test_worker_fanout.py`: 3 workers run via `asyncio.gather` under `asyncio.Semaphore(4)`; each emits `agent="<stage>.<worker>"` events; artifacts written under `<stage>/<worker_slug>/<file>.md`; merged `StageOutput` unions worker artifacts/evidence; reviewer evaluates merged output.
- [ ] Implement `_engine/workers.py::run_workers_parallel(profile, stage, ctx) -> StageOutput`.
- [ ] Stage node: when `stage.workers` non-empty, delegate to workers; else current single-call ReAct path.
- [ ] Add `worker_outputs` to `RunState`.
- [ ] Commit `feat(v1.6): worker subagent fanout per stage`.

### Task 11 — Per-stage reviewer prompts + skill injection
- [ ] Author 15 reviewer_stage*.md files (40–80 lines each) per Section 6.4 of spec.
- [ ] Tests `tests/integration/test_per_stage_reviewer.py`: when `reviewer_prompt_for_stage[slug]` set, that file is loaded; per-stage skills injected; falls back to shared `reviewer.md` when absent.
- [ ] Edit `_engine/nodes/reviewer.py` for resolution + injection.
- [ ] Update `pricing/profile.py` and `profitability/profile.py` to point `reviewer_prompt_package` at their own packages and populate `reviewer_prompt_for_stage`.
- [ ] Commit `feat(v1.6): per-stage reviewer prompts with skill injection`.

### Task 12 — DB migration `0010_run_lifecycle_columns`
- [ ] Tests `tests/integration/test_run_lifecycle_columns.py`: columns exist, default NULL, partial index `ix_runs_status_heartbeat` present.
- [ ] Author migration; `down_revision="0009_enable_pricing"`.
- [ ] Edit `app/models/run.py`: add three columns.
- [ ] Run `alembic upgrade head` in test DB.
- [ ] Commit `feat(v1.6): run lifecycle columns and partial heartbeat index`.

### Task 13 — Heartbeat task + run timeout in worker
- [ ] Tests:
  - `test_run_lifecycle_columns.py` extended: `started_at` set on start, `heartbeat_at` bumped, `completed_at` set on terminal status.
  - `tests/integration/test_run_timeout.py`: with `RUN_TIMEOUT_SECONDS=2` and a sleeping stage, run marked failed within 5 s with reason `"timeout: exceeded 2 s budget"`.
- [ ] Implement heartbeat coroutine in `app/workers/run_worker.py` started in `continue_after_framing`, cancelled in `finally:`.
- [ ] Implement timeout via `asyncio.wait_for` wrapping the graph invocation.
- [ ] Commit `feat(v1.6): heartbeat task and per-run timeout enforcement`.

### Task 14 — Stale-run recovery sweeper
- [ ] Tests `tests/integration/test_run_recovery.py`: row with `status='running' AND heartbeat_at < now()-5min` marked failed, `system.run_failed` published with reason `"staleness: no heartbeat for >5 minutes"`, `completed_at` set.
- [ ] Implement `_engine/recovery.py::sweep_stale_runs(session)`.
- [ ] Wire into `app/main.py` lifespan startup.
- [ ] Commit `feat(v1.6): stale-run recovery sweeper at startup`.

### Task 15 — Strict scripted-model guard
- [ ] Tests `tests/integration/test_scripted_model_guard.py`: raw POST `/runs/start` with `model_factory` payload → 400; `default_model_factory` raises if invoked with sentinel `__test__` marker outside test mode.
- [ ] Edit `app/api/runs.py` + `app/workers/run_worker.py` to enforce.
- [ ] Document fixture pattern in `backend/tests/conftest.py` docstring.
- [ ] Commit `feat(v1.6): strict scripted-model guard in production path`.

### Task 16 — Backfill script
- [ ] Tests `tests/integration/test_backfill_stale_runs.py`: 5 stale rows + 2 fresh rows → 5 marked failed + 5 system.run_failed events; rerun → 0 changes (idempotent).
- [ ] Implement `backend/scripts/backfill_stale_runs.py` (CLI: `python -m scripts.backfill_stale_runs`).
- [ ] Run against live DB after PR merge (note in PR description; not in CI).
- [ ] Commit `feat(v1.6): backfill orphaned V1.5 stale runs`.

### Task 17 — Author worker prompts (37 net-new)
- [ ] No new tests beyond `validate()` from Task 6.
- [ ] Author market_entry stage3/4/5 worker prompts (8 files, 40–80 lines each).
- [ ] Author pricing × 5 × 3 = 15 worker prompts.
- [ ] Author profitability × 5 × 3 = 15 worker prompts.
- [ ] Each prompt: role, scope (one stage focus area), required tool use (cite ≥2 web sources), output format reminder (StageOutput).
- [ ] Commit in three logical commits:
  - `feat(v1.6): author market_entry stage3-5 worker prompts`
  - `feat(v1.6): author pricing worker prompts`
  - `feat(v1.6): author profitability worker prompts`

### Task 18 — Wire workers into all three profiles
- [ ] Tests: profile `validate()` now passes with workers populated; `tests/integration/test_*_v16_smoke.py` for each type assert ≥4 artifacts in ≥3 stages and ReAct tool use happened.
- [ ] Edit `market_entry/profile.py`, `pricing/profile.py`, `profitability/profile.py`: populate `required_skills` per Section 5.1, `workers` per Section 6, `reviewer_prompt_for_stage`, `reviewer_skills_per_stage`, `framing_skills`, `synthesis_skills`, `audit_skills`.
- [ ] Author smoke tests with scripted model honouring tool calls + per-worker `StageOutput`.
- [ ] Commit `feat(v1.6): wire skills and workers into all three profiles`.

### Task 19 — Frontend: child worker nodes
- [ ] Tests `frontend/__tests__/useAgentStates.workers.test.tsx`: events with `agent="stage1_foundation.market_sizing"` produce `nodes["stage1_foundation"].children["market_sizing"]`.
- [ ] Tests `frontend/__tests__/AgentGraph.workers.test.tsx`: child nodes render under parent; collapse/expand toggle works.
- [ ] Edit `frontend/lib/useAgentStates.ts`: split on `.`; attach to parent.
- [ ] Edit `frontend/components/AgentGraph.tsx`: collapsible mini-cards under stage parent (reuse existing layout primitives; do not pull in new viz lib).
- [ ] Commit `feat(v1.6): render worker child nodes in agent graph`.

### Task 20 — Docs + final gate + PR
- [ ] Update `AGENTS.md` (top-level) with V1.6 section: skill injection, worker fanout, run lifecycle, ReAct loop.
- [ ] Update `backend/app/agents/agent.md` with new module map.
- [ ] Create/update `backend/app/skills/agent.md` listing 20 packs and stage assignments.
- [ ] Run `CHECK_BUILD=1 bash scripts/check.sh` — all green.
- [ ] Manual smoke: spawn one run per type via local stack, verify ≥4 artifacts in ≥3 stages and child worker nodes render in `AgentGraph`.
- [ ] Run `python -m scripts.backfill_stale_runs --dry-run` against staging DB; capture output for PR description.
- [ ] Push branch; open PR; include forensic findings table + before/after metrics expectations.
- [ ] Final commit: `docs(v1.6): update agent.md files for skills and workers`.

---

## Acceptance Gate (re-stated from spec §9)
1. `CHECK_BUILD=1 bash scripts/check.sh` green.
2. `pytest backend/tests` passes; ≥17 net-new tests added.
3. Manual smoke per profile: ≥4 artifacts in ≥3 stages; `final_report.md` ≥4 KB; worker child nodes render.
4. Backfill script results recorded in PR.
5. AGENTS.md and agent.md files updated.
