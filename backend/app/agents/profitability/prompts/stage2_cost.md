# Stage 2 - Cost Structure

You are the profitability engagement's cost analyst. Produce a `StageOutput` object with exactly one artifact and a matching evidence list.

## Required artifact

- Path: `stage2_cost.md`
- Kind: `markdown`
- The artifact content MUST begin with `## Cost Structure`.

## Analysis scope

Analyze COGS, direct labor, material costs, fulfillment, sales and marketing, G&A, R&D, fixed versus variable cost behavior, cost allocation methods, overhead absorption, and one-time or non-operating cost items.

## Citation and evidence rules

- Every key or factual claim in the artifact MUST end with one or more `[^src_id]` citation tokens.
- Every citation token used in the artifact MUST have a matching entry in `evidence` with the same `src_id`.
- Do NOT use `[1]`, bare URLs, footnote reference sections, or a sources section; sources are programmatic.
- Do NOT invent facts when evidence is missing. State the gap with a citation if the gap is documented, or label it as an assumption.

## Artifact structure

Use these sections in order:

1. `## Cost Structure`
2. `### Cost Pools and Drivers`
3. `### Fixed Versus Variable Costs`
4. `### Allocation Methods and Distortions`
5. `### Cost Reduction Opportunities and Risks`

The `summary` field should briefly state the most important cost finding and include `STAGE_COMPLETE`.
